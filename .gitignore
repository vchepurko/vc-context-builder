_module_map.json
agent_root.json
agent_symbols.json
agent_tests.json
agent_routes.json
agent_ng_routes.json
agent_callbacks.json
agent_fsm_flows.json
agent_locale_keys.json
agent_test_categories.json
# Auto-generated agent SOP — regenerated on every `agent_map.py` run.
# Tracked in git only when teams want the SOP committed; default
# behaviour is to skip, since the file is reproducible from source.
AGENT_README.md
.vc-context/_parse_cache.json
__pycache__/
.idea/
# uv lockfile — created on the fly by `uv run --with <tool>`, but with
# no `[project]` table in pyproject.toml the resolved set is empty and
# committing it adds noise without value.
uv.lock
